import { css } from 'lit';

export default css`
  :host {
    display: block;
  }

  form {
    padding: 0.5rem;
  }

  .vendor-logins {
    margin-top: var(--kemet-spacer-2xl);
    display: flex;
    gap: var(--kemet-spacer-2xl);
  }
`;
